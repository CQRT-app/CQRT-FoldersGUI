from setuptools import setup

setup(
    name='CQRT_Folders_GUI',
    version='1.1',
    python_requires='>3.7.8',
    install_requires=[
        'passlib >= 1.7.4',
        'Kivy >= 2.0.0',
        'kivymd >= 0.104.2'
    ],
)
